package scopus.translator.replacement

+ scopus.lib.Rec

* AnnotatedLine(line: String, isComment: Boolean)

/**
 * This preprocessor annotates lines to determine whether they are comments.
 */
* CommentPreprocessor() {

  ScopusBeginComment = "/*"
  ScopusEndComment = "*/"

  annotate_lines(lines: Seq[String]): Seq[AnnotatedLine] = {
    result =
      Rec().foldLeft(lines, initval, op)
        .annotated_lines_rev
        .reverse

    * RecPair(comment_state: Boolean, annotated_lines_rev: Seq[AnnotatedLine])

    initval = RecPair(false, Seq())

    op(pair: RecPair, line: String): RecPair = {
      (current_state, new_comment_state) = annotate_this_line(line, pair.comment_state)
      RecPair(new_comment_state, pair.annotated_lines_rev.+:(AnnotatedLine(line, current_state)))
    }

    annotate_this_line(line: String, comment_state: Boolean): (Boolean, Boolean) =
      if comment_state
      then (true, not line.trim.endsWith(ScopusEndComment))
      else
        if line.trim.startsWith(ScopusBeginComment)
        then (true, not line.trim.endsWith(ScopusEndComment))
        else (false, false)

    result
  }

}

