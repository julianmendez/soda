package soda.example.mathematics

class FiboExampleInSoda

  apply (n : Int) =
    _rec (n) (0) (1)

  _rec (m : Int) (a : Int) (b : Int) : Int =
    if m == 0 then a
    else if m == 1 then b
    else _rec (m - 1) (b) (a + b)

end
