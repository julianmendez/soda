package soda.example.miniexample

class MiniExample
  extends
    soda.lib.Recursion

  run () =
    range (50)
      .map (lambda x --> print (" " + (2 * x + 1) ) )

end

class Main

  main (arguments : Array [String] ) : Unit =
    MiniExample_ ().run ()

end

