package soda.translator.example

* ScalaReservedWordEscaping() {

  var = "var"

  val = 1

  def[A, B](key: A, value: B): Pair[A, B] = Pair(key, value)

  while[A, B](seq: Seq[A], cond: A -> Boolean, do : A -> B): Seq[B] =
    seq.takeWhile(cond).map(do)

  protected = "protected"
  private = "private"

  * Pair[A, B](key: A, value: B)

}

