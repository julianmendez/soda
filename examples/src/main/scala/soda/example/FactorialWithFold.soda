package soda.translator.example


* FactorialWithFold = {
  + soda.lib.Recursion_

  get_factorial(n: Int): Int =
    Recursion_().fold(
      Recursion_().range(n), 1, (product, k) -> product * (k + 1) )
}

* FactorialWithFold_() extends FactorialWithFold

